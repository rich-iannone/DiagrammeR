#' Select a graph for a pipeline
#' @description Select a graph and introduce that graph to
#' a set of pipeline operations.
#' @param graph a graph of class \code{dgr_graph}
#' @export select_graph

select_graph <- function(graph){

  # Return the graph object
  return(graph)
}
