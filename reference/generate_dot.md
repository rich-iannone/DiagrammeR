# Generate DOT code using a graph object

Generates Graphviz DOT code as an R character object using DiagrammeR
graph object.

## Usage

``` r
generate_dot(graph)
```

## Arguments

- graph:

  A graph object of class `dgr_graph`.

## Value

A character vector of length 1 containing Graphviz DOT code.
